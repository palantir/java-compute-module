pluginManagement {
    repositories {
        mavenCentral() { metadataSources { mavenPom(); ignoreGradleMetadataRedirection() } }
    }
}

buildscript {
    dependencies { classpath 'com.palantir.gradle.jdks:gradle-jdks-settings:0.73.0' }
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}
apply plugin: 'com.palantir.jdks.settings'

rootProject.name = 'java-compute-module'

include 'lib'

throw new RuntimeException('''The excavator failed to run properly - exited with code 1 and output:\n\n''' + file('gradle/exception').text + '\n\n')
